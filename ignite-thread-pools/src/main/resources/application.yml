spring:
  application:
    name: ignite-thread-pools
    
logging:  file:    name: /var/tmp/logs/${spring.application.name}/${spring.application.name}
  level:    root: info
    poc.ignite: debug